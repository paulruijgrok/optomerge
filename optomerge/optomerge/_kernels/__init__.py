"""
optomerge._kernels
==================
Private numerical core: the validated, function-based image-processing
routines (I/O, normalisation, background subtraction, channel segmentation,
phase-correlation registration, affine warping). These are an implementation
detail of the object-oriented public API and are not part of the supported
interface — import from the top-level ``optomerge`` package instead.
"""
